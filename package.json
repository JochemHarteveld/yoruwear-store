{
  "name": "yoruwear-store",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix client run start & npm --prefix server run dev",
    "build": "npm --prefix client run build",
    "test": "npm --prefix client run test"
  }
}
